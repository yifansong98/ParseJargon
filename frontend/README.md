# Frontend (Chrome Extension)

Project-level setup instructions are in:

- [../README.md](../README.md)

Quick commands:

```powershell
npm install
npm run build
```

Then load `frontend/build` via `chrome://extensions` -> `Load unpacked`.
